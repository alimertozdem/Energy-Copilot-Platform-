# Notebook 11 (HVAC engine) — Page 7 gold fixes · 2026-06-21

Three source edits to `notebooks/gold/11_hvac_analytics_engine.py`. Each fixes at the
source so the existing cascade (insulation → efficiency → renovation; cop → scop)
recomputes naturally. Apply in the **Fabric** notebook (line numbers may differ from
this repo copy — use the FIND anchors). Re-run notebook 11 → re-run the materialize
cell → mv_hvac_analytics refreshes.

Verified ground truth (mv_hvac_analytics, 2025): cop_actual_avg = 400/400 NULL;
insulation_score = 0 for B002/B006/B008 and 100 for B007; 2023 heating=cooling=0 (HDD/CDD=0).

---

## Edit 1 — G1: COP rated-proxy (approved 2026-06-21)

Actual COP is not measured (BMS/IoT = Phase 2) so `cop_actual_avg` was left NULL and
the "HVAC Avg COP" card renders blank. The notebook's own comment promised a
`cop_rated` fallback that was never coded. Fill it for the heat-pump family.

**FIND** (the SCOP block, ~line 735):
```python
df_with_scop = df_heat_loss.withColumn(
    "scop_rolling",
```

**INSERT immediately ABOVE that block:**
```python
# G1 (2026-06-21): COP rated-proxy. Actual COP unmeasured (BMS=Phase 2) -> cop_actual_avg
# was NULL -> COP card blank. Fill from rated spec for the heat-pump family so the COP
# card + SCOP populate. Labelled "rated (spec)" in the report. Gas/district/chiller stay NULL.
df_heat_loss = df_heat_loss.withColumn(
    "cop_actual_avg",
    when(col("system_type").isin("heat_pump", "gshp", "wshp"),
         coalesce(col("cop_actual_avg"), col("cop_rated")))
    .otherwise(col("cop_actual_avg"))
)
```

Result: B001 = 3.8, B007 = 5.5, B010 = 4.8 (flat across months). SCOP = same rated value
(rolling avg of a constant). Portfolio Avg COP card ≈ 4.7. Others stay NULL (correct —
not heat pumps). hvac_efficiency_score HP branch already coalesced to cop_rated → unchanged.

> If the cell errors with `cop_rated` not found, the column is named differently at this
> point in the Fabric notebook — tell me and I'll re-anchor it to silver_building_master.

---

## Edit 2 — G2: insulation_score 0-anchor recalibration (approved 2026-06-21)

The per-element score maps `2 × GEG benchmark U` → 0 points. GEG targets are strict
(wall 0.24), so 2×GEG = 0.48; every building above that floors to 0. Result: B002 (U 0.8),
B006 (0.9) and B008 (1.85) all score 0 — indistinguishable. Move the 0-anchor to realistic
un-renovated pre-1977 stock U (TABULA/IWU typology) so poor buildings spread.

**FIND** (~line 344):
```python
    target_2x = 2.0 * target      # 2× GEG (0 puan)
```

**REPLACE with:**
```python
    target_2x = {"wall": 2.0, "roof": 1.5, "floor": 1.2, "window": 5.0}[elem]  # G2: pre-1977 worst-stock U = 0 puan
```

Result (approx): B002 ~40, B006 ~38 (were 0), B008 ~0–5 (genuinely worst: U 1.85 + ACH 8.5),
B007/B010 ~90–100, B001 ~72 (≈ unchanged). Cascade: gas-building hvac_efficiency_score
(= insulation_score × 0.45) lifts off 0 too (e.g. B002 0 → ~18). Portfolio efficiency/insulation
cards will rise slightly from 46 / 48.

---

## Edit 3 — G3: drop 2023 (no weather data)

2023 has HDD = CDD = 0 (degree-day/weather not ingested for 2023), so the heating/cooling
split (`total × sensitivity × hdd_fraction`) yields 0 → V1 shows a sham single-series 2023.
2023 cannot be split without weather, so drop those rows from the HVAC gold.

**FIND** the statement that builds `df_final` via `.select(...)` (~line 924).

**ADD a new line immediately AFTER that statement:**
```python
df_final = df_final.filter(col("reporting_year") >= 2024)  # G3: 2023 has no HDD/CDD -> no heat/cool split; drop sham rows
```

Result: gold_hvac_analytics = 2024-01 onward (360 rows for 10 buildings × 36 months).
V1 starts at 2024 with a real heating/cooling/ventilation split.

> Alternative (more work, deferred): back-fill 2023 HDD/CDD via
> `notebooks/ingestion/02_openmeteo_weather_loader.py` to keep 2023 HVAC coverage.

---

## After applying

1. Fabric: Run notebook 11 (Run All) — no errors.
2. Fabric: re-run the materialize loop cell (the one with `gold_hvac_analytics` in TABLES).
3. Ping me — I re-verify mv_hvac_analytics: COP non-null for B001/B007/B010, insulation
   spread (no 3×0 pile-up), 2023 gone, then we move to the report-level measures (R1 CO2, R2 badge).
