/**
 * DEPRECATED (Day 24) -- intentionally empty.
 *
 * The per-page report view is no longer rendered here. It now lives in the
 * persistent section shell (reports/ReportSectionShell.tsx, mounted by
 * reports/layout.tsx) so the Power BI embed stays mounted across page
 * navigation. The route's page.tsx only validates the slug now.
 */
export {}
